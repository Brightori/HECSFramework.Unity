using HECSFramework.Core;

namespace Commands
{
    [Documentation(Doc.Visual, "Sent when we need to respawn view from ViewReferenceGameObject")]
    public struct RespawnViewCommand : ICommand
    {
    }
}